{
  "name": "integra-web",
  "version": "1.0.0",
  "private": true,
  "description": "Integra web frontend — REPL calculator with calculus, fractals, and graphing",
  "scripts": {
    "build": "tsc",
    "pages:dev": "wrangler pages dev .",
    "pages:deploy": "npm run build && wrangler pages deploy .",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
