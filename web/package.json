{
  "name": "integra-web",
  "version": "1.0.0",
  "private": true,
  "description": "Integra web frontend — REPL calculator with calculus, fractals, and graphing",
  "scripts": {
    "build": "tsc",
    "pages:dev": "wrangler pages dev .",
    "pages:deploy": "npm install && npm run build && npx wrangler pages deploy .",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "wrangler": "^4.0.0"
  }
}
