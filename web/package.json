{
  "name": "integra-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Integra web calculator — Rust WASM + Astro",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run build:wasm && npm run build:site",
    "build:site": "astro build",
    "build:wasm": "cd ../wasm && wasm-pack build --target web --release && rm -rf ../web/src/wasm && cp -r pkg ../web/src/wasm",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^7.0.0"
  }
}
